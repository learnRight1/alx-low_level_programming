#include <stdio.h>

/**
 * main -  a program that prints the alphabet in lowercase.
 * Return: Always 0
 */
int main(void)
{
	char lc;

	lc = 'a';
	while
		(lc <= 'z') {
			putchar(lc);
		}

	putchar("\n");

	return (0);
}
